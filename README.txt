# WebAR — Snake Charmer Costume (MindAR face)

Что это: мобильная веб-страница, которая накладывает поверх лица целый костюм (мультяшный стиль).

## Деплой (быстро)
1) Залей папку на Netlify / Vercel / GitHub Pages (нужен HTTPS).
2) Открой URL на телефоне → разреши камеру.

## Тонкая настройка посадки
Открывай `index.html`, у узла `<a-plane>` меняй:
- `width`/`height` — масштаб;
- `position="0 -0.68 0.02"` — вертикальный сдвиг:
   - больше по модулю — ниже;
   - меньше — выше.
Сохрани и перезагрузи страницу.

## QR-код
Сгенерируй QR с URL сайта (любой генератор), раздай — у людей откроется камера с костюмом.

Удачи! 🎭
